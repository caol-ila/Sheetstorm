# Orchestration Log: Banner — CR#4/5/9 Security Hardening

**Date:** 2026-03-31T00:54:17Z  
**Agent:** Banner (Backend Engineer)  
**Model:** claude-sonnet-4.6 (background)  
**Task:** CORS Configuration + Rate-Limiting + Error-Code Standardization

## Manifest

**Input Scope:**
- CR#4 — CORS configurable (was hardcoded AllowAnyOrigin)
- CR#5 — Substitute token rate-limiting (new requirement)
- CR#9 — Error codes (BAND_NOT_FOUND → FORBIDDEN/403 for non-members)
- TDD-driven, maintain 900+ test suite

**Execution:**
- Status: DONE ✅
- Tests: 914 passing (+32 new tests)
- Pre-existing failures: 1 (unrelated, unchanged)

## Deliverables

### 1. CORS Configuration (CR#4)

**Architecture:**
- Removed hardcoded `AllowAnyOrigin()` from `Program.cs`
- Added configurable CORS policy via `appsettings*.json`
- Environment variable support: `CORS_ALLOWED_ORIGINS` (semicolon-separated)
- Development: `["*"]` (localhost development)
- Production: Empty by default (must be explicitly configured)

**Files Modified:**
- `src/Sheetstorm.Api/Program.cs` — CORS policy registration
- `src/Sheetstorm.Api/appsettings.json` — Cors section (empty)
- `src/Sheetstorm.Api/appsettings.Development.json` — Cors: ["*"]
- `tests/Sheetstorm.Tests/TestWebApplicationFactory.cs` *(new)*
- `tests/Sheetstorm.Tests/Api/CorsConfigurationTests.cs` *(new)*

**Test Coverage:**
- 3 integration tests: default prod (rejected), dev mode (allowed), env override

**Key Pattern:**
- `UseSetting("Cors:AllowedOrigins", value)` in WebApplicationFactory for test overrides
- ConfigureAppConfiguration NOT used (applied too late for startup reads)

### 2. Rate-Limiting on Substitute Tokens (CR#5)

**Architecture:**
- Added FixedWindowRateLimiter (10 requests/min per IP) to `Program.cs`
- Policy name: `substitute-validate`
- Applied via `[EnableRateLimiting("substitute-validate")]` on endpoint

**Files Modified:**
- `src/Sheetstorm.Api/Program.cs` — Rate-limiting policy registration
- `src/Sheetstorm.Api/Controllers/SubstituteAccessController.cs` — ValidateToken endpoint

**Test Coverage:**
- 2 integration tests: within limit (200), exceed limit (429 TooManyRequests)

**Key Pattern:**
- WebApplicationFactory per test class (xUnit creates new instance per test → fresh limiter state)

### 3. Error-Code Standardization (CR#9)

**Change:**
- `BandAuthorizationService.RequireMembershipAsync()` now throws `DomainException("FORBIDDEN", 403)` instead of `DomainException("BAND_NOT_FOUND", 404)`
- Semantic: "You exist, band exists, but you're not a member" = 403 Forbidden, not 404 Not Found
- `BandService.RemoveMemberAsync()` updated to check caller's membership first

**Impact:**
- 17+ downstream tests updated to expect 403 instead of 404
- All legitimate resource-not-found 404s (MEMBER_NOT_FOUND, CONFIG_NOT_FOUND, PIECE_NOT_FOUND) left unchanged
- Batch replacement required careful exclusion of legitimate 404s

**Files Modified:**
- `src/Sheetstorm.Infrastructure/Auth/BandAuthorizationService.cs` — RequireMembershipAsync error code
- `src/Sheetstorm.Infrastructure/Band/BandService.cs` — Caller membership check
- 15+ test files — Updated assertions

## Cross-Team Integration

**For Strange (Pagination):**
- CORS + rate-limiting compatible with paginated endpoints
- Rate-limit applies uniformly to all substitute validation requests

**For Romanoff (Flutter):**
- CORS configuration allows frontend to connect (Development: all origins, Production: env-configured)
- 403 responses on non-member requests handled properly by error dialog

**For Parker (QA):**
- Integration test patterns (CORS, rate-limiting) established
- Test fixtures updated for new 403 semantics

## Files Modified

- `src/Sheetstorm.Api/Program.cs`
- `src/Sheetstorm.Api/appsettings.json`
- `src/Sheetstorm.Api/appsettings.Development.json`
- `src/Sheetstorm.Api/Controllers/SubstituteAccessController.cs`
- `src/Sheetstorm.Infrastructure/Auth/BandAuthorizationService.cs`
- `src/Sheetstorm.Infrastructure/Band/BandService.cs`
- `tests/Sheetstorm.Tests/TestWebApplicationFactory.cs` *(new)*
- `tests/Sheetstorm.Tests/Api/CorsConfigurationTests.cs` *(new)*
- `tests/Sheetstorm.Tests/Substitutes/SubstituteRateLimitTests.cs` *(new)*
- 15+ test files (assertions updated)

## Verification

```
$ dotnet test
914 tests PASSED (+32 new)
1 pre-existing failure (unrelated)
All 3 CRs completed
```

---

**Status:** ✅ **COMPLETE**

All 3 code reviews resolved in parallel with Strange's pagination work.
