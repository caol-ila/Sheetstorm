name: PR Multi-Model Review

# TODO: Wire up Claude Opus 4.6 + Sonnet 4.6 + GPT-5.4 reviewers (Framework-Spec §7.3)

on:
  pull_request:
    branches: [main]

jobs:
  placeholder:
    runs-on: ubuntu-latest

    steps:
      - name: Reviewer Stub
        run: echo "TODO: Multi-model review implementation"
